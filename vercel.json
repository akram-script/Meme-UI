{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/meme-ui",
  "nodeVersion": "20.x",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
